name: Release Plugin Asset

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to upload asset to, for example v0.1.0
        required: true
        type: string

permissions:
  contents: write

jobs:
  upload-plugin-zip:
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ github.event.release.tag_name || inputs.tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build plugin zip asset
        run: |
          mkdir -p package
          cp -r octoprint_brickweave package/
          cp setup.py MANIFEST.in README.md pyproject.toml package/
          cd package
          zip -r ../OctoPrint-BrickWeave.zip .

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          files: OctoPrint-BrickWeave.zip
          overwrite_files: true
